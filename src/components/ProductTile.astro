---
import { withBase } from "../lib/base";

interface Props {
  image: string;
  alt: string;
}

const { image, alt } = Astro.props;
---

<div class="product-tile">
  <div class="product-image-box">
    <img src={withBase(image)} alt={alt} loading="lazy" />
    <h5>{alt}</h5>
  </div>
</div>
